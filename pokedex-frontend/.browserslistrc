# Browserslist configuration used by the build system to adjust CSS and JS output to support the specified browsers below.
# For additional information regarding the format and rule options, please see:
#   https://github.com/browserslist/browserslist#queries
#
# For the full list of supported browsers by the Angular framework, please see:
#   https://angular.io/guide/browser-support
#
# You can see what browsers were selected by your queries by running:
#   npx browserslist
#
# Notes (informational only):
# - Do NOT modify the query lines below unless you understand the compatibility impact on build output.
# - Security & deployment guidance (not enforced here):
#     * Restrict allowed CORS origins to known frontends and drive that list from environment configuration.
#     * Require authentication and authorization for write endpoints on the server-side.
#     * Store and rotate secrets using a secure secret manager (Vault, cloud KMS/Secret Manager, etc.).
# These notes are provided to help teams follow best practices; they do not change build behavior.

last 1 Chrome version
last 1 Firefox version
last 2 Edge major versions
last 2 Safari major versions
last 2 iOS major versions
Firefox ESR
not IE 11 # Angular supports IE 11 only as an opt-in. To opt-in, remove the 'not' prefix on this line.
